import {fakeContainers} from "../FakeData";

export const containerData = fakeContainers();

export const DEFAULT_DISPLAY_COLUMNS = ['name', 'creator', 'created', 'barcode', 'organization', 'location'];

